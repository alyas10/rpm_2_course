---
order: 3
title: Задание 3
---

Вариант - 1\
Создать класс `Animal` с полями: тип животного (например, `"собака"`), кличка, издаваемый звук (например, `"..."`). Реализовать метод `MakeSound()`, выводящий звук.\
Создать производный класс `Dog` с полем -- порода. Переопределить метод `MakeSound()` на `"Гав!"`. Добавить метод `Bark()`, выводящий `"Собака {кличка} породы {порода} гавкает!"`.

Вариант - 2\
Создать класс `Animal` с полями: кличка, издаваемый звук. Реализовать метод `MakeSound()`.\
Создать производный класс `Cat` с полем -- окрас (строка). Переопределить метод `MakeSound()` на `"Мяу!"`. Добавить метод `Purr()`, выводящий `"Кот {кличка} мурлычет"`.

Вариант - 3\
Создать класс `Animal` с полями: тип животного, кличка. Реализовать метод вывода базовой информации.\
Создать производный класс `Bird` с полем -- может летать (логический тип). Добавить метод `Fly()`, выводящий `"Птица {кличка} летает!"` или `"Птица {кличка} не умеет летать"`, в зависимости от значения поля.

Вариант - 4\
Создать класс `Animal` с полями: кличка, издаваемый звук (по умолчанию `"..."`). Реализовать метод `MakeSound()`.\
Создать производный класс `Fish` с полем -- среда обитания (`"пресная вода"` или `"морская вода"`). Добавить метод `Swim()`, выводящий `"Рыба {кличка} плывёт в {среда}"`.

Вариант - 5\
Создать класс `Animal` с полями: тип животного, вес (в кг). Реализовать метод вывода информации.\
Создать производный класс `Pet` с полем -- имя хозяина. Переопределить метод вывода, добавив информацию о хозяине: `"Это {кличка}, вес {вес} кг, хозяин — {имя}"`.

Вариант - 6\
Создать класс `Animal` с полями: кличка, возраст (в годах). Реализовать метод вывода информации.\
Создать производный класс `Reptile` с полем -- оптимальная температура содержания (в °C). Добавить метод `NeedsHeating()`, возвращающий `true`, если температура \< 25 °C.

Вариант - 7\
Создать класс `Animal` с полями: кличка, издаваемый звук. Реализовать метод `MakeSound()`.\
Создать производный класс `Parrot` с полем -- запомненная фраза (строка). Переопределить метод `MakeSound()` на эту фразу. Добавить метод `Speak()`, выводящий `"Попугай {кличка} говорит: {фраза}"`.

Вариант - 8\
Создать класс `Animal` с полями: тип животного, кличка. Реализовать метод вывода информации.\
Создать производный класс `Rabbit` с полем -- цвет глаз. Добавить метод `Hop()`, выводящий `"Кролик {кличка} с {цвет} глазами прыгает!"`.

Вариант - 9\
Создать класс `Animal` с полями: кличка, издаваемый звук. Реализовать метод `MakeSound()`.\
Создать производный класс `Horse` с полем -- масть (строка: `"гнедая"`, `"рыжая"`, `"серая"` и др.). Добавить метод `Gallop()`, выводящий `"Лошадь {кличка} масти {масть} скачет галопом!"`.

Вариант - 10\
Создать класс `Animal` с полями: кличка, продолжительность жизни (в годах). Реализовать метод вывода информации.\
Создать производный класс `Insect` с полем -- количество лап. Добавить метод `Crawl()`, выводящий `"Насекомое {кличка} с {N} лапами ползёт"`.

Вариант - 11\
Создать класс `Animal` с полями: имя, издаваемый звук. Реализовать метод `MakeSound()`.\
Создать производный класс `Hamster` с полем -- любит колесо (логический тип). Добавить метод `RunInWheel()`, выводящий `"Хомяк {имя} бежит в колесе"` или `"Хомяк {имя} не интересуется колесом"`.

Вариант - 12\
Создать класс `Animal` с полями: тип животного, кличка. Реализовать метод вывода базовой информации.\
Создать производный класс `ExoticPet` с полем -- страна происхождения. Добавить метод `GetOrigin()`, выводящий `"Питомец {кличка} ({тип}) родом из {страна}"`.